{
  "name": "calendar-event-creator",
  "version": "1.2.0",
  "description": "Chrome extension that creates Google Calendar events from selected text",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:ui": "npx playwright test --ui",
    "eval:prompt": "deno test --allow-net --allow-env supabase/functions/_shared/llm-prompt.eval.ts",
    "deploy:backend": "npx supabase functions deploy process-text",
    "deploy:backend:image": "npx supabase functions deploy process-image",
    "deploy": "./scripts/deploy.sh",
    "package": "./scripts/package-extension.sh"
  },
  "keywords": [
    "chrome-extension",
    "google-calendar",
    "openai"
  ],
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
